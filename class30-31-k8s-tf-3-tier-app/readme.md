# Things to do in class8

# some rsoirecs on Fargate
-> create a namspace -> Terraform
-> fargate profile (use that name space ) -> Terraform
-> push a deployment on fargate with Trraform

# some on nodes(managd nodes)

-> deploy the 3-toer app on EKS

-> AWS load balancer controller -> with Terraform
-> backend and frontent with menifest (using kuberctl) -> use argocd later
-> secrts, config from Terraform 
-> ingress Terraform 
-> automate ALB to dns mapping with Terraform
-> automat th ACM ssl cert issue and https ingress




Class 9 Agenda

Terraform stuff
- rds
- config map for rds endpoint 
- secrt for database url

from menifsts